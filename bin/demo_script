#!/usr/bin/env python

import asyncio
import seattle_sunrise

if __name__ == "__main__":
    loop = asyncio.get_event_loop()
    loop.run_until_complete(seattle_sunrise.light_loop(loop))
    loop.close()
